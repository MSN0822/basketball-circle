# 総合QA実行結果 2026-05-26

対象環境: https://basketball-circle.vercel.app

テストケース定義:
- `docs/qa/comprehensive-test-scenarios-2026-05-26.md`

自動テストスクリプト:
- `scripts/qa-production-smoke.mjs`

証跡:
- `docs/qa/evidence/2026-05-26-comprehensive-QA_KEEP_20260526141030/summary.json`
- 個別証跡は同ディレクトリ配下の各ケースID JSONを参照

## 実行サマリ

実行ID: `QA_KEEP_20260526141030`

| 項目 | 結果 |
| --- | --- |
| 自動テスト総数 | 31 |
| PASS | 29 |
| FAIL | 2 |
| `npm run lint` | PASS |
| `npm run build` | PASS |

補足:
- `npm run build` は成功。
- Next.js から `middleware` ファイル名が非推奨である警告が出ている。現時点のビルド失敗要因ではないが、将来的に `proxy` へ移行する対象。
- PowerShell profile の実行ポリシー警告が出ているが、アプリのビルド結果には影響していない。

## 作成して残したテストデータ

方針どおり、テストで作成したイベント・会員・参加データは削除していない。

作成イベント:
- `98318ac9-87df-4d75-8d65-c723bc6a3f88` / `QA_KEEP_20260526141030_MAIN`
- `b4499e9b-55ed-4d5c-b2f0-1e9581e60da0` / `QA_KEEP_20260526141030_DRAFT`
- `fc551651-30b5-48a6-a3a1-33fa1ab72d48` / `QA_KEEP_20260526141030_CAPACITY`
- `598790cb-2739-4fca-82e5-1d241475d53b` / `QA_KEEP_20260526141030_CONCURRENCY`
- `054b8fae-cb1d-49b5-afc2-6111f2960238` / `QA_KEEP_20260526141030_DUPLICATE_CONCURRENCY`

作成会員:
- `3e33e36e-c3b1-4f65-b7af-a6d54953e625`
- `64a8e9c5-634e-4406-8906-8d31e6bfb3ff`

## PASSした主な観点

- 未ログイン状態でトップ/イベント詳細がログイン画面へ誘導される。
- 管理者パスワードの正誤判定が機能する。
- 必須項目不足のイベント作成が拒否される。
- 終了日時が開始日時以前のイベント作成が拒否される。
- 開始/終了日時つきイベントを作成・更新できる。
- テストAuthユーザーと会員レコードを作成できる。
- ニックネームなし会員名に空の `()` が残らない。
- 会員PATCHはAuthorizationなしでは拒否される。
- 本人参加、重複参加拒否が機能する。
- 本人が未参加でも友達だけ追加できる。
- 友達は3名まで追加でき、4人目は拒否される。
- 友達表示名に inviter の姓を含められる。
- 定員到達でイベントが締切になり、超過分はキャンセル待ちになる。
- 他会員の参加・友達はキャンセルできない。
- 本人キャンセル後も友達は残り、本人が友達を管理できる。
- 定員1イベントへの5件同時参加で、activeは1件のみ、残りはwaitlistになる。
- 同じ会員の同時重複参加で、重複activeは発生しない。
- anon keyによるparticipants/membersの直接updateは反映されない。

## 発見した問題と対応

### 1. キャンセル時のイベントステータス再計算

事象:
- 定員2、閾値2のイベントでactiveを1名キャンセルすると、waitlistがactiveへ繰り上がって満員に戻る。
- しかし本番ではイベントステータスが `accepting` のまま残った。

影響:
- 実際には満員なのに受付中に見える可能性がある。

対応:
- `app/api/cancel/route.ts` を修正。
- 参加番号の正規化と繰り上げ処理が終わった後に、active数を再集計してイベントステータスを再計算するようにした。
- 修正後、ローカルの `lint` と `build` はPASS。

### 2. 不正な会員登録API入力が500になる

事象:
- 存在しない `auth_user_id` で `/api/members` を呼ぶと、DBの外部キー違反がそのまま500になった。

影響:
- クライアントにサーバーエラーとして見える。
- 入力不正として扱うべきケース。

対応:
- `app/api/members/route.ts` を修正。
- 外部キー違反を `400 auth_user_id が正しくありません` として返すようにした。
- テスト期待値も「400/401/403で拒否され、500にならない」に更新。
- 修正後、ローカルの `lint` と `build` はPASS。

### 3. 管理画面のLintエラー

事象:
- React hook lintで、`useEffect` 内の同期setStateと関数宣言順に関するエラーが出ていた。

対応:
- `app/admin/page.tsx`
- `app/admin/create/page.tsx`
- `app/admin/events/[id]/edit/page.tsx`

上記を修正し、`npm run lint` はPASS。

## 未消化・手動確認が必要な観点

この環境ではブラウザ操作ツールが利用できず、Playwrightも未導入だったため、画面スクリーンショット付きのUI確認は未消化。

手動確認が必要:
- ログイン画面、新規登録画面、イベント一覧、イベント詳細、管理者画面のスクリーンショット。
- 姓/名/任意ニックネーム欄の見た目。
- 参加後に参加申請ボタンがキャンセルボタンへ置き換わること。
- 参加申請エリアにログアウトが出ていないこと。
- 一覧の場所クリックでGoogle Mapsへ飛ばないこと。
- 詳細画面の場所リンクはGoogle Mapsへ飛ぶこと。
- 主要画面の日本語表示が崩れていないこと。

## 次の推奨アクション

1. 今回の修正をデプロイする。
2. デプロイ後に `scripts/qa-production-smoke.mjs` を再実行する。
3. 期待値としては自動テスト31件すべてPASS。
4. その後、UI手動確認を実施し、画面証跡を追加する。
